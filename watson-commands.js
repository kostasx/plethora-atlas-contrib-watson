// Generated by CoffeeScript 1.10.0
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('watson').description('IBM Watson related utilities').option('--speech-to-text <audio>', 'Convert <audio> file to text. Maximum size is 100MB.').option('--output <JSON>', 'Write the output to a JSON file.').action(function(options) {
    var Watson;
    Watson = require('./watson');
    if (options.speechToText) {
      Watson.init(config.keys.watson['speech-to-text']);
      return Watson.speechToText({
        audio_file: options.speechToText,
        output: options.output || null
      }).then(function(res) {
        return console.log(res);
      })["catch"](console.log);
    }
  });
};

module.exports = initCommands;
